name: Init

on:
  push:
    branches:
      - main

jobs:
  BUILD_IMAGE_ECR:
    uses: ./.github/workflows/image-ecr.yml
    secrets: inherit

  Deploy_EKS:
     uses: ./.github/workflows/tf-eks.yml
     secrets: inherit
     needs: BUILD_IMAGE_ECR
